# テストリスト（TDD用）

## patterns.js のテスト

### 基本機能
- [ ] `grid` パターンが指定サイズで描画される
- [ ] `dots` パターンが指定色で描画される
- [ ] `stripes-horizontal` パターンが描画される
- [ ] `stripes-vertical` パターンが描画される
- [ ] `checkerboard` パターンが描画される
- [ ] `diagonal` パターンが描画される
- [ ] `zigzag` パターンが描画される
- [ ] `hexagon` パターンが描画される
- [ ] `wave` パターンが描画される
- [ ] `triangle` パターンが描画される

### エッジケース
- [ ] 存在しないパターン名でエラーを返す
- [ ] 無効な色指定でデフォルト色を使用する
- [ ] ゼロまたは負のサイズで描画しない

## pdf-utils.js のテスト

### PDF生成
- [ ] A4サイズ（210x297mm）のPDFが生成される
- [ ] 2ページのPDFが生成される
- [ ] ページ1に柄パターンが描画される
- [ ] ページ2に折り線・トンボが描画される
- [ ] ユーザー画像が含まれる場合、ページ1に描画される
- [ ] 画像位置とスケールが正しく適用される

### 寸法検証
- [ ] BOOK_DIMENSIONS定数が正しい値を持つ
- [ ] PDF内の座標計算が正確である

## app.js のテスト（E2E）

### UI操作
- [ ] パターン選択でプレビューが更新される
- [ ] 色変更でプレビューが更新される
- [ ] 画像ドロップで画像がプレビューに表示される
- [ ] 画像位置スライダーで画像が移動する
- [ ] 画像スケールスライダーで画像が拡大縮小する
- [ ] PDFダウンロードボタンでファイルが生成される

### 状態管理
- [ ] 初期状態が正しく設定される
- [ ] 状態変更がプレビューに反映される
